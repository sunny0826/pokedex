# Optional. Used by the Android client to check for remote Pokédex data-package
# updates. The public web app talks to PokeAPI directly and does not need this.
# VITE_POKEDEX_DATA_MANIFEST_URL=https://example.com/pokedex_zh_data.manifest.json
